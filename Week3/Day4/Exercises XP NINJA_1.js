var intYear1 = 1980;
var intYear2 = 2013;

var intResult = Math.max(intYear1, intYear2) + Math.abs(intYear1-intYear2);

alert("The date when the younger one is exactly half the age of the older is: " + intResult);